"""text-to-sql — natural-language questions to SQL over a real SQLite DB, scored by execution
accuracy (do generated and gold SQL return the same rows?), benchmarking prompting strategies."""
__version__ = "0.1.0"
